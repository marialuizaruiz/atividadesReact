import React from 'react';

export default function Avatar() {
    
    const avatar = '';
    const description = '';
    return (
        <img 
        className="avatar" 
        src={avatar} 
        alt={description}
        />
    );
}